# Installing GeoZones

This module is for use with the [SilverStripe](https://silverstripe.org) Framework.

This module is managed via [Composer](https://getcomposer.org/). If your project
does no use Compoer, you will need to configure this.

Once setup, install this module via composer, using:

    composer require silvercommerce/geozones